---
phase: 07-calendar-bookings-clients
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - frontend/src/pages/admin/Clients.jsx
  - frontend/src/pages/admin/NewBookingDialog.jsx
  - frontend/src/pages/admin/Calendar.jsx
  - frontend/src/App.jsx
autonomous: true

must_haves:
  truths:
    - "Admin can search clients by phone and see matching results"
    - "Admin can register a new client with name, phone, and optional email"
    - "Admin can view a client's appointment history"
    - "Admin can create a booking through a multi-step flow: client -> service -> professional -> slot -> confirm"
    - "New booking appears on the calendar immediately after creation"
  artifacts:
    - path: "frontend/src/pages/admin/Clients.jsx"
      provides: "Client search, registration, and appointment history"
      min_lines: 100
    - path: "frontend/src/pages/admin/NewBookingDialog.jsx"
      provides: "Multi-step booking creation dialog"
      min_lines: 150
  key_links:
    - from: "frontend/src/pages/admin/Clients.jsx"
      to: "/api/admin/clients"
      via: "useQuery with search param for client list, POST for registration"
      pattern: "apiFetch.*admin/clients"
    - from: "frontend/src/pages/admin/NewBookingDialog.jsx"
      to: "/api/bookings/availability"
      via: "apiFetch POST for slot query"
      pattern: "apiFetch.*bookings/availability"
    - from: "frontend/src/pages/admin/NewBookingDialog.jsx"
      to: "/api/bookings"
      via: "apiFetch POST for booking creation + PATCH confirm"
      pattern: "apiFetch.*bookings"
---

<objective>
Build the Client management page (search, register, appointment history) and the multi-step booking creation dialog. Wire the Clients route and integrate the "New Booking" button on the Calendar page.

Purpose: Completes the operational interface -- admins need to manage clients and create bookings from the calendar. The booking flow is the most critical user journey: search/register client, pick service, pick professional, select slot, confirm.

Output: Working Clients page and booking creation flow integrated into the Calendar.
</objective>

<execution_context>
@/Users/maxcardoso/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maxcardoso/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-calendar-bookings-clients/07-01-SUMMARY.md
@frontend/src/App.jsx
@frontend/src/pages/admin/Services.jsx
@frontend/src/lib/api.js
@frontend/src/components/layout/Sidebar.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Clients page with search, registration, and appointment history</name>
  <files>frontend/src/pages/admin/Clients.jsx, frontend/src/App.jsx</files>
  <action>
  Create `frontend/src/pages/admin/Clients.jsx` following the same CRUD page pattern as Services.jsx and Users.jsx:

  **Client list with search:**
  - useQuery with queryKey `['clients', searchTerm]` calling `apiFetch('/admin/clients?search=' + encodeURIComponent(searchTerm) + '&limit=50')`
  - Search input at top with debounced state (use a simple approach: controlled input + useEffect with 300ms setTimeout to update searchTerm state that triggers the query). The search param filters by phone or name on the backend.
  - Table columns: Name, Phone, Email, Actions
  - "View History" button on each row

  **Register client dialog:**
  - "New Client" button at top right (same pattern as "New Service" in Services.jsx)
  - Dialog with form: Name (required), Phone (required), Email (optional)
  - useMutation POST to `/admin/clients` with body `{ name, phone, email }`
  - On success: invalidateQueries(['clients']), toast.success, close dialog
  - On error: show error in dialog (phone duplicate -> 409 conflict)

  **Phone lookup shortcut:**
  - When search term looks like a phone number (digits only, 8+ chars), show a "Look up exact phone" button/link
  - Clicking it calls `apiFetch('/admin/clients/by-phone/' + phone)`
  - If found, show the client directly (highlight in list or open detail)
  - If 404, offer to register with that phone pre-filled

  **Appointment history panel:**
  - When "View History" is clicked, open a Dialog showing the client's appointment history
  - Fetch from `/admin/clients/${clientId}/appointments`
  - Show list of appointments: date, time, service name, professional name, status badge
  - Use the same status color Badge pattern as the calendar
  - Sort by date descending (most recent first)
  - Empty state: "No appointments found"

  **In frontend/src/App.jsx:**
  - Add import: `import Clients from '@/pages/admin/Clients';`
  - Add route in ADMIN children: `{ path: '/admin/clients', element: <Clients /> }`

  Note: If Calendar.jsx import was added in Plan 02, just add the Clients route alongside it. If Plan 02 and 03 run in the same wave, the executor of Plan 03 should read App.jsx first to see if Calendar was already added, and add both if needed.
  </action>
  <verify>
  - `cd frontend && npm run build` succeeds
  - Navigate to /admin/clients — page renders with search input and table
  - Search by phone shows filtered results
  - "New Client" opens dialog, submitting creates client and it appears in list
  - "View History" opens dialog with appointment list (may be empty if no bookings exist)
  </verify>
  <done>Clients page supports search by phone/name (FCLNT-01), registration of new clients (FCLNT-02), and viewing appointment history (FCLNT-03). Route is wired in App.jsx.</done>
</task>

<task type="auto">
  <name>Task 2: Build multi-step booking creation dialog and integrate on Calendar</name>
  <files>frontend/src/pages/admin/NewBookingDialog.jsx, frontend/src/pages/admin/Calendar.jsx</files>
  <action>
  Create `frontend/src/pages/admin/NewBookingDialog.jsx` as a multi-step dialog component:

  **Props:** `{ open, onOpenChange, onBookingCreated }`

  **Steps (tracked with useState `step` = 1-5):**

  **Step 1 — Client selection:**
  - Phone input field for search
  - "Search" button that calls `apiFetch('/admin/clients/by-phone/' + phone)`
  - If found: show client name + phone, "Select" button to proceed
  - If 404: show inline registration form (name, phone pre-filled, email optional)
    - Submit calls `apiFetch('/admin/clients', { method: 'POST', body: { name, phone, email } })`
    - On success: auto-select the new client and proceed
  - Store selected client in state: `{ id, name, phone }`
  - "Next" button advances to step 2

  **Step 2 — Service selection:**
  - Fetch services from `/admin/services` (useQuery, queryKey ['services'])
  - Show list of active services as clickable cards or radio-style list
  - Each shows: name, duration (Xmin), price (R$ X.XX)
  - Selecting a service stores it and advances to step 3

  **Step 3 — Professional selection:**
  - Fetch professionals from `/admin/professionals` (useQuery, queryKey ['professionals'])
  - Filter to show only professionals who offer the selected service (check `professional.services` array if the backend includes it, otherwise show all active professionals)
  - Each shows: name, assigned services
  - Selecting a professional advances to step 4

  **Step 4 — Date and slot selection:**
  - Date input (HTML date input, default to today)
  - When date is selected, fetch availability: `apiFetch('/bookings/availability', { method: 'POST', body: { professionalId, serviceId, date } })`
  - IMPORTANT: The availability endpoint is under `/bookings/availability` (apiKey auth). But we're on the admin frontend using JWT. We need to either:
    a) Call the admin bookings endpoint (doesn't have availability)
    b) Add an admin availability proxy
    c) The vite proxy forwards /api/* to the backend — check if the backend has admin-accessible availability

  Actually, looking at the backend: `POST /api/bookings/availability` uses `apiKeyAuth`. The frontend uses JWT auth. These are different auth mechanisms. The frontend CANNOT call this endpoint.

  **Solution:** The executor must add a new route `POST /api/admin/bookings/availability` in `src/routes/admin/bookings.js` that mirrors the existing availability endpoint but uses adminAuth. Import `getAvailableSlots` from bookingService and call it the same way. Add Zod validation for body: `{ professionalId: uuid, serviceId: uuid, date: YYYY-MM-DD regex }`.

  Then call `apiFetch('/admin/bookings/availability', { method: 'POST', body: { professionalId, serviceId, date } })`.

  - Display available slots as a grid of time buttons (e.g., "09:00", "09:30", "10:00"...)
  - Each slot shows start time. Clicking selects it.
  - If no slots: show "No available slots for this date" message
  - If result has `reason` (NOT_WORKING, FULLY_BOOKED): show appropriate message
  - Selected slot stored as `{ startTime, endTime }` (from the slot data)

  **Step 5 — Confirmation:**
  - Show summary: Client name + phone, Service name + price, Professional name, Date + time slot
  - "Confirm Booking" button
  - On click:
    1. Create pre-reservation: `apiFetch('/admin/bookings', { method: 'POST', body: { clientId, serviceId, professionalId, startTime, endTime, idempotencyKey: crypto.randomUUID() } })`
       WAIT — the create booking endpoint is `POST /api/bookings` with apiKeyAuth. Need admin version too.
       **Solution:** Add `POST /api/admin/bookings` in admin bookings route. Import `createPreReservation` from bookingService. Same Zod validation. Returns 201.
    2. Immediately confirm: `apiFetch('/admin/bookings/' + booking.id + '/status', { method: 'PATCH', body: { status: 'CONFIRMED' } })`
    3. On success: toast.success("Booking created"), call onBookingCreated(), close dialog
    4. On error: toast.error with message

  **Navigation:**
  - "Back" button on steps 2-5 to go to previous step
  - Step indicator at top (Step 1 of 5, Step 2 of 5, etc.) — simple text, nothing fancy
  - When dialog closes (onOpenChange false), reset all state to step 1

  **Backend additions needed (in this task):**
  Add to `src/routes/admin/bookings.js`:
  1. `POST /availability` — mirrors /api/bookings/availability but with adminAuth. Import `getAvailableSlots` from bookingService.
  2. `POST /` — mirrors /api/bookings POST but with adminAuth. Import `createPreReservation` from bookingService.

  Place these routes BEFORE the `/:id/status` route to avoid param conflicts.

  **In Calendar.jsx:**
  - Import NewBookingDialog
  - Add state: `[newBookingOpen, setNewBookingOpen] = useState(false)`
  - Add "New Booking" button next to the page title (same position pattern as "New Service" in Services.jsx)
  - Pass `onBookingCreated={() => queryClient.invalidateQueries({ queryKey: ['bookings'] })}` to refresh calendar
  </action>
  <verify>
  - `cd frontend && npm run build` succeeds
  - On Calendar page, "New Booking" button is visible
  - Click "New Booking" opens multi-step dialog
  - Step 1: can search client by phone, register new if not found
  - Step 2: service list shows active services
  - Step 3: professional list shows active professionals
  - Step 4: date picker + available slots grid (requires backend running with seed data)
  - Step 5: summary + confirm creates booking, calendar refreshes
  </verify>
  <done>Multi-step booking creation dialog (FCAL-03) works end-to-end: client search/register, service selection, professional selection, slot selection, confirmation. New bookings appear on calendar immediately. Admin availability and booking creation endpoints added to backend.</done>
</task>

</tasks>

<verification>
- Clients page: search by phone returns results, register new client works, appointment history displays
- Booking flow: all 5 steps complete successfully end-to-end
- New booking appears on calendar after creation
- All admin endpoints (availability, create booking, client operations) work with JWT auth
- No regressions on existing calendar or dashboard functionality
</verification>

<success_criteria>
- FCLNT-01: Admin can search clients by phone on the Clients page
- FCLNT-02: Admin can register new clients via dialog
- FCLNT-03: Admin can view client appointment history
- FCAL-03: Admin can create booking via 5-step flow (client -> service -> professional -> slot -> confirm)
- New booking appears on calendar immediately after creation
</success_criteria>

<output>
After completion, create `.planning/phases/07-calendar-bookings-clients/07-03-SUMMARY.md`
</output>

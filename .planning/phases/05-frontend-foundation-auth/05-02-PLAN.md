---
phase: 05-frontend-foundation-auth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/adminUserService.js
  - src/routes/admin/users.js
  - src/services/clientService.js
  - src/routes/admin/clients.js
  - src/services/bookingService.js
  - src/routes/admin/bookings.js
  - src/routes/admin/dashboard.js
  - src/app.js
autonomous: true

must_haves:
  truths:
    - "GET /api/admin/users returns a list of admin users with id, name, email, role, active, createdAt"
    - "POST /api/admin/users creates a new admin user with hashed password"
    - "PATCH /api/admin/users/:id updates user name, role, or active status"
    - "GET /api/admin/clients returns paginated client list with optional search"
    - "PATCH /api/admin/bookings/:id/status transitions a booking status"
    - "GET /api/admin/dashboard/stats returns today's bookings count, total clients, active professionals, pending payments"
    - "User management endpoints require ADMIN role"
  artifacts:
    - path: "src/services/adminUserService.js"
      provides: "Admin user CRUD service layer"
      exports: ["listAdminUsers", "createAdminUser", "updateAdminUser"]
    - path: "src/routes/admin/users.js"
      provides: "Admin user management routes"
    - path: "src/routes/admin/clients.js"
      provides: "Client listing for admin"
    - path: "src/routes/admin/bookings.js"
      provides: "Booking status update for admin"
    - path: "src/routes/admin/dashboard.js"
      provides: "Dashboard stats endpoint"
  key_links:
    - from: "src/routes/admin/users.js"
      to: "src/services/adminUserService.js"
      via: "service function imports"
      pattern: "import.*adminUserService"
    - from: "src/app.js"
      to: "src/routes/admin/users.js"
      via: "route mounting"
      pattern: "app\\.use.*admin/users"
---

<objective>
Backend API endpoints needed by the frontend: user management CRUD, client listing, booking status update, and dashboard stats.

Purpose: The frontend needs these endpoints to render the admin dashboard, user management page, and support future phases (client list, booking actions). Building all backend endpoints now means frontend plans can focus purely on UI.

Output: 4 new route files, 1 new service file, 2 service additions, all mounted in app.js.
</objective>

<execution_context>
@/Users/maxcardoso/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maxcardoso/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-foundation-auth/05-RESEARCH.md
@src/routes/admin/services.js
@src/services/clientService.js
@src/services/bookingService.js
@src/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin user service + routes</name>
  <files>src/services/adminUserService.js, src/routes/admin/users.js</files>
  <action>
1. Create src/services/adminUserService.js with three functions:
   - `listAdminUsers()`: findMany with select (id, name, email, role, active, createdAt), ordered by name asc. Do NOT return password field.
   - `createAdminUser({ name, email, password, role })`: hash password with bcrypt (rounds: 10), create record, return same select fields. Let Prisma unique constraint on email throw naturally (the error handler will catch it).
   - `updateAdminUser(id, updates)`: findUnique first, throw NotFoundError if missing, then update with given fields. Return same select fields. Do NOT allow password update through this function (strip it from updates if present).

   Import bcrypt from 'bcryptjs', prisma from '../lib/prisma.js', NotFoundError from '../lib/errors.js'.

2. Create src/routes/admin/users.js following the EXACT project route pattern:
   - Import Router from 'express', z from 'zod'
   - Import adminAuth from '../../middleware/auth.js'
   - Import requireRole from '../../middleware/auth.js'
   - Define local asyncHandler
   - Apply `router.use(adminAuth)` then `router.use(requireRole('ADMIN'))` -- ALL user management is admin-only
   - GET / -- list all admin users
   - POST / -- create user, validate body: name (string 2-200), email (string email), password (string min 6), role (enum ADMIN/RECEPTIONIST). Return 201.
   - PATCH /:id -- update user, validate params: id (uuid), body: name (optional), role (optional enum), active (optional boolean). Return 200.

   Response format: `{ data: ... }` matching existing patterns.
  </action>
  <verify>
    Run `node -e "import('./src/services/adminUserService.js').then(m => console.log(Object.keys(m)))"` -- should print the three exported functions.
    Run `node -e "import('./src/routes/admin/users.js').then(m => console.log(typeof m.default))"` -- should print "function" (Router).
  </verify>
  <done>Admin user CRUD service and routes created, admin-only access enforced via requireRole('ADMIN').</done>
</task>

<task type="auto">
  <name>Task 2: Client list, booking status, dashboard stats endpoints + app.js wiring</name>
  <files>src/services/clientService.js, src/routes/admin/clients.js, src/services/bookingService.js, src/routes/admin/bookings.js, src/routes/admin/dashboard.js, src/app.js</files>
  <action>
1. Add `listClients({ search, page, limit })` to src/services/clientService.js:
   - Build a where clause: if search provided, use OR with name contains (insensitive) and phone contains
   - Use findMany with pagination: skip = (page - 1) * limit, take = limit
   - Also count total matching records
   - Return `{ clients, total, page, limit }`

2. Create src/routes/admin/clients.js:
   - Follow project route pattern (Router, adminAuth, local asyncHandler)
   - GET / with query validation: search (optional string), page (coerce number, default 1, min 1), limit (coerce number, default 20, min 1, max 100)
   - Call listClients(req.query), return { data: result }

3. Add `updateBookingStatus(id, status)` to src/services/bookingService.js:
   - Find booking by id, throw NotFoundError if missing
   - Update status field
   - Return updated booking (include client and service relations in select)

4. Create src/routes/admin/bookings.js:
   - Follow project route pattern
   - PATCH /:id/status with validation: params id (uuid), body status (enum: CONFIRMED, COMPLETED, CANCELLED, NO_SHOW)
   - Call updateBookingStatus, return { data: booking }

5. Create src/routes/admin/dashboard.js:
   - Follow project route pattern
   - GET /stats -- query today's booking count (CONFIRMED + PRE_RESERVED), total clients, active professionals, pending payments count
   - Use Promise.all for parallel queries
   - Return { data: { todayBookings, totalClients, totalProfessionals, pendingPayments } }

6. Mount all new routes in src/app.js:
   - Import and mount: `/api/admin/users` -> usersRouter, `/api/admin/clients` -> clientsRouter, `/api/admin/bookings` -> bookingsRouter, `/api/admin/dashboard` -> dashboardRouter
   - Place these AFTER existing route mounts, BEFORE error handler
   - Do NOT move or modify existing route mounts
  </action>
  <verify>
    Start the server: `cd /Users/maxcardoso/Projects/Service-schedule && timeout 5 node src/server.js 2>&1 || true` -- should start without import errors.
    Check routes mounted: `grep -c "app.use" src/app.js` -- should show increased count from before.
  </verify>
  <done>Four new admin endpoints operational: GET /api/admin/users, GET /api/admin/clients, PATCH /api/admin/bookings/:id/status, GET /api/admin/dashboard/stats. All mounted in app.js.</done>
</task>

</tasks>

<verification>
- `grep "adminUserService" src/routes/admin/users.js` returns match
- `grep "listClients" src/services/clientService.js` returns match
- `grep "updateBookingStatus" src/services/bookingService.js` returns match
- `grep "admin/users" src/app.js` returns match
- `grep "admin/dashboard" src/app.js` returns match
- Server starts without errors
</verification>

<success_criteria>
- All 4 new route files exist and follow the project route pattern
- adminUserService.js exports listAdminUsers, createAdminUser, updateAdminUser
- clientService.js has listClients with pagination
- bookingService.js has updateBookingStatus
- All routes mounted in app.js
- User routes require ADMIN role
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-foundation-auth/05-02-SUMMARY.md`
</output>

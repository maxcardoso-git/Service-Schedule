---
phase: 05-frontend-foundation-auth
plan: 05
type: execute
wave: 4
depends_on: ["05-04"]
files_modified:
  - frontend/src/components/layout/AppLayout.jsx
  - frontend/src/components/layout/Sidebar.jsx
  - frontend/src/components/layout/Header.jsx
  - frontend/src/pages/admin/Dashboard.jsx
  - frontend/src/App.jsx
autonomous: true

must_haves:
  truths:
    - "Admin user sees sidebar with links: Dashboard, Services, Professionals, Calendar, Clients, Users"
    - "Receptionist user sees sidebar with only: Receptionist link"
    - "Header shows current user name and a working logout button"
    - "Clicking logout clears session and redirects to login"
    - "Dashboard page shows 4 stat cards loaded from /api/admin/dashboard/stats"
    - "Navigation links highlight the current active route"
  artifacts:
    - path: "frontend/src/components/layout/AppLayout.jsx"
      provides: "Main layout with Sidebar + Header + Outlet"
      min_lines: 15
    - path: "frontend/src/components/layout/Sidebar.jsx"
      provides: "Role-filtered navigation sidebar"
      min_lines: 30
    - path: "frontend/src/components/layout/Header.jsx"
      provides: "Top header with user info and logout"
      min_lines: 15
    - path: "frontend/src/pages/admin/Dashboard.jsx"
      provides: "Dashboard with stat cards from API"
      min_lines: 30
  key_links:
    - from: "frontend/src/components/layout/Sidebar.jsx"
      to: "frontend/src/stores/authStore.js"
      via: "reads user.role to filter nav items"
      pattern: "useAuthStore.*role"
    - from: "frontend/src/pages/admin/Dashboard.jsx"
      to: "frontend/src/lib/api.js"
      via: "fetches dashboard stats"
      pattern: "apiFetch.*dashboard"
    - from: "frontend/src/App.jsx"
      to: "frontend/src/components/layout/AppLayout.jsx"
      via: "router wraps protected routes in AppLayout"
      pattern: "AppLayout"
---

<objective>
Application layout with role-based navigation and dashboard page.

Purpose: Admin and receptionist users see different navigation based on their role. The dashboard serves as the landing page with real-time stats. This plan establishes the visual shell that all future pages render inside.

Output: AppLayout (sidebar + header + content area), role-filtered Sidebar, Header with logout, Dashboard page with stat cards.
</objective>

<execution_context>
@/Users/maxcardoso/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maxcardoso/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-foundation-auth/05-RESEARCH.md
@.planning/phases/05-frontend-foundation-auth/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppLayout, Sidebar, and Header components</name>
  <files>frontend/src/components/layout/AppLayout.jsx, frontend/src/components/layout/Sidebar.jsx, frontend/src/components/layout/Header.jsx</files>
  <action>
1. Create frontend/src/components/layout/Sidebar.jsx:
   - Define navigation items array with: label, path, icon (from lucide-react), roles (which roles see this item)
   - Admin items: Dashboard (LayoutDashboard, '/'), Services (Scissors, '/admin/services'), Professionals (Users, '/admin/professionals'), Calendar (Calendar, '/admin/calendar'), Clients (UserSearch, '/admin/clients'), Users (Shield, '/admin/users')
   - Receptionist items: Receptionist (ClipboardList, '/receptionist')
   - Read user.role from useAuthStore
   - Filter items: show item if item.roles includes user.role, OR if item.roles is not defined (visible to all)
   - Use NavLink from react-router-dom for active state styling
   - Active link: bg-accent text-accent-foreground, inactive: text-muted-foreground hover:text-foreground
   - Sidebar width: w-64, fixed left, full height, border-r, bg-background
   - Show app name "Service Schedule" at top of sidebar

2. Create frontend/src/components/layout/Header.jsx:
   - Read user from useAuthStore
   - Show user.name (or user.email as fallback) on the right side
   - Show role badge (small text, muted color)
   - Logout button (LogOut icon from lucide-react): calls useAuthStore.getState().logout(), then navigates to '/login' using useNavigate
   - Header: h-16, border-b, bg-background, flex items-center justify-between, px-6

3. Create frontend/src/components/layout/AppLayout.jsx:
   - Render Sidebar on the left
   - Main content area: ml-64 (offset for fixed sidebar), min-h-screen
   - Header at top of main content area
   - Outlet below header with padding (p-6)
   - Import Sidebar, Header, Outlet from react-router-dom
  </action>
  <verify>
    Run `cd /Users/maxcardoso/Projects/Service-schedule/frontend && npm run build 2>&1` -- should compile without errors.
  </verify>
  <done>AppLayout renders sidebar + header + content area. Sidebar filters navigation by user role. Header shows user info and logout button.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard page + router update</name>
  <files>frontend/src/pages/admin/Dashboard.jsx, frontend/src/App.jsx</files>
  <action>
1. Create frontend/src/pages/admin/Dashboard.jsx:
   - Use useQuery from @tanstack/react-query to fetch stats: queryKey ['dashboard-stats'], queryFn calls `apiFetch('/admin/dashboard/stats')`
   - Display 4 stat cards using shadcn Card component:
     - "Bookings Today" (Calendar icon) -- data.data.todayBookings
     - "Total Clients" (Users icon) -- data.data.totalClients
     - "Active Professionals" (UserCheck icon) -- data.data.totalProfessionals
     - "Pending Payments" (CreditCard icon) -- data.data.pendingPayments
   - Cards in a responsive grid: grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4
   - Show loading skeleton while fetching (simple animate-pulse divs, no need for shadcn Skeleton)
   - Show error state if query fails
   - Page title: "Dashboard" as h1

2. Update frontend/src/App.jsx router to use AppLayout:
   - Import AppLayout
   - Restructure router:
     - /login -> Login (no layout)
     - ProtectedRoute wrapper -> AppLayout wrapper ->
       - / (index) -> Dashboard
       - /admin/users -> ProtectedRoute with roles={['ADMIN']} -> Users (placeholder div for now, "Users page coming in next plan")
     - * -> NotFound
   - Keep QueryClientProvider, Toaster, and initFromToken as before

IMPORTANT: The /admin/users route needs a nested ProtectedRoute with roles=['ADMIN']. Use the pattern from the research file where ProtectedRoute is used as a layout route (element, not component) with children.
  </action>
  <verify>
    Run `cd /Users/maxcardoso/Projects/Service-schedule/frontend && npm run build 2>&1` -- should compile without errors.
    Check dashboard file: `grep "useQuery" frontend/src/pages/admin/Dashboard.jsx` -- should match.
    Check router: `grep "AppLayout" frontend/src/App.jsx` -- should match.
  </verify>
  <done>Dashboard page fetches and displays 4 stat cards from API. Router wraps protected routes in AppLayout. Admin-only routes additionally check role.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` completes without errors
- Sidebar.jsx filters nav items by role
- Header.jsx shows user info and logout
- Dashboard.jsx fetches from /api/admin/dashboard/stats
- App.jsx router uses AppLayout for all protected routes
- Admin-only routes use nested ProtectedRoute with roles
</verification>

<success_criteria>
- Admin sees 6 nav items in sidebar (Dashboard, Services, Professionals, Calendar, Clients, Users)
- Receptionist sees only Receptionist nav item
- Current route is visually highlighted in sidebar
- Logout button clears session and redirects to login
- Dashboard loads and displays 4 stat cards with live data
- Layout is responsive (sidebar, content area)
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-foundation-auth/05-05-SUMMARY.md`
</output>

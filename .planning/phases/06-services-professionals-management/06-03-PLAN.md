---
phase: 06-services-professionals-management
plan: 03
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - frontend/src/pages/admin/Professionals.jsx
autonomous: true

must_haves:
  truths:
    - "Admin can configure a professional's working hours via a visual weekly grid"
    - "Working hours grid shows Mon-Sun with start/end time inputs per day"
    - "Saving working hours calls PUT /api/admin/professionals/:id/working-hours"
    - "Grid loads existing working hours when opened"
  artifacts:
    - path: "frontend/src/pages/admin/Professionals.jsx"
      provides: "WorkingHoursDialog component"
      contains: "WorkingHoursDialog"
  key_links:
    - from: "frontend/src/pages/admin/Professionals.jsx"
      to: "/api/admin/professionals/:id/working-hours"
      via: "PUT mutation on save"
      pattern: "working-hours"
    - from: "frontend/src/pages/admin/Professionals.jsx"
      to: "/api/admin/professionals/:id"
      via: "GET to load current working hours"
      pattern: "admin/professionals/"
---

<objective>
Add working hours configuration to the Professionals page via a visual weekly grid dialog.

Purpose: Admins need to set each professional's weekly schedule (FMGMT-06) so the availability engine generates correct slots.
Output: WorkingHoursDialog added to Professionals.jsx with Mon-Sun grid, time inputs, and save functionality.
</objective>

<execution_context>
@/Users/maxcardoso/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maxcardoso/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/maxcardoso/Projects/Service-schedule/.planning/phases/06-services-professionals-management/06-02-SUMMARY.md
@/Users/maxcardoso/Projects/Service-schedule/src/routes/admin/professionals.js (PUT /:id/working-hours endpoint already exists, accepts { hours: [{ dayOfWeek, startTime, endTime }] })
@/Users/maxcardoso/Projects/Service-schedule/src/services/professionalService.js (getProfessionalById returns workingHours, replaceWorkingHours does full replace)
@/Users/maxcardoso/Projects/Service-schedule/frontend/src/pages/admin/Professionals.jsx (add WorkingHoursDialog here)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WorkingHoursDialog to Professionals page</name>
  <files>frontend/src/pages/admin/Professionals.jsx</files>
  <action>
  Add a WorkingHoursDialog component to the existing Professionals.jsx file. Add a Clock icon button to the Actions column of each professional row (next to the edit and services buttons).

  **WorkingHoursDialog (controlled, receives `professional` prop):**

  State management:
  - `hours` state: array of 7 objects, one per day (0=Sunday through 6=Saturday), each with `{ enabled: bool, startTime: string, endTime: string }`
  - Initialize all days as disabled with default times "09:00" / "18:00"

  On open (when professional prop changes to non-null):
  - Fetch professional detail: `apiFetch('/admin/professionals/' + professional.id)` to get current workingHours
  - Populate `hours` state from response: for each workingHour entry, set that dayOfWeek's enabled=true, startTime, endTime
  - Days without entries remain disabled

  UI layout:
  - Dialog with title "Working Hours - {professional.name}", wider: `className="sm:max-w-lg"`
  - 7 rows, one per day, ordered Mon(1), Tue(2), Wed(3), Thu(4), Fri(5), Sat(6), Sun(0)
  - Each row: day label (fixed width) | Switch to enable/disable | startTime Input (type="time") | "-" separator | endTime Input (type="time")
  - When day is disabled, time inputs are visually muted and disabled
  - Use DAY_LABELS constant: `['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday']`
  - Display order: [1,2,3,4,5,6,0] (Mon first, Sun last) -- this is just display order, the dayOfWeek values stay 0-6

  Save button:
  - Collect only enabled days into array: `{ dayOfWeek, startTime, endTime }` for each enabled day
  - Mutation: PUT to `/admin/professionals/${professional.id}/working-hours` with `{ hours: [...] }`
  - On success: invalidateQueries(['professionals']), toast.success("Working hours saved"), close dialog
  - On error: show error message in dialog (same pattern as edit dialogs)
  - Validate before submit: for each enabled day, startTime must be before endTime (string comparison works for HH:MM). Show inline error if invalid.

  **Integration with main component:**
  - Add state: `const [hoursProf, setHoursProf] = useState(null)`
  - Add Clock button in Actions column: `<Button variant="ghost" size="icon-sm" onClick={() => setHoursProf(professional)} title="Working hours"><Clock className="h-4 w-4" /></Button>`
  - Import Clock from lucide-react
  - Render: `<WorkingHoursDialog professional={hoursProf} open={hoursProf !== null} onOpenChange={(val) => { if (!val) setHoursProf(null); }} />`
  </action>
  <verify>
  1. Run `cd /Users/maxcardoso/Projects/Service-schedule/frontend && npx vite build --mode development 2>&1 | tail -5` -- should compile without errors
  2. Grep for "WorkingHoursDialog" in Professionals.jsx to confirm it exists
  3. Grep for "working-hours" in Professionals.jsx to confirm API call
  </verify>
  <done>Working hours dialog shows Mon-Sun grid with enable toggle and time inputs per day. Save calls PUT /working-hours with enabled days only. Existing hours load on open.</done>
</task>

</tasks>

<verification>
1. Frontend builds without errors
2. Clock button appears in professional row actions
3. Dialog loads existing working hours from GET /professionals/:id
4. Saving sends PUT /professionals/:id/working-hours with correct format
5. Only enabled days are included in the save payload
</verification>

<success_criteria>
- Admin can click Clock icon on any professional to open working hours dialog
- Dialog shows Mon-Sun with toggle + time pickers per day
- Existing working hours are pre-loaded
- Admin can enable/disable days and set start/end times
- Save persists to backend and shows success toast
</success_criteria>

<output>
After completion, create `.planning/phases/06-services-professionals-management/06-03-SUMMARY.md`
</output>

---
phase: 06-services-professionals-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/serviceService.js
  - src/routes/admin/services.js
  - frontend/src/pages/admin/Services.jsx
  - frontend/src/App.jsx
autonomous: true

must_haves:
  truths:
    - "Admin can view all services (including inactive) in a searchable table"
    - "Admin can create a new service with name, duration, price"
    - "Admin can edit an existing service's name, duration, price"
    - "Admin can toggle a service's active status with one click"
  artifacts:
    - path: "src/services/serviceService.js"
      provides: "listAllServices function"
      contains: "listAllServices"
    - path: "src/routes/admin/services.js"
      provides: "GET / endpoint for admin service listing"
      contains: "router.get"
    - path: "frontend/src/pages/admin/Services.jsx"
      provides: "Services CRUD page"
      exports: ["default"]
    - path: "frontend/src/App.jsx"
      provides: "Route for /admin/services"
      contains: "/admin/services"
  key_links:
    - from: "frontend/src/pages/admin/Services.jsx"
      to: "/api/admin/services"
      via: "apiFetch in useQuery"
      pattern: "apiFetch.*admin/services"
    - from: "src/routes/admin/services.js"
      to: "serviceService.listAllServices"
      via: "import and route handler"
      pattern: "listAllServices"
---

<objective>
Build the Services management page -- backend list endpoint + full CRUD frontend.

Purpose: Admins need to visually manage services (FMGMT-01, FMGMT-02) instead of using direct API calls.
Output: Working Services page at /admin/services with table, create/edit dialogs, and active toggle.
</objective>

<execution_context>
@/Users/maxcardoso/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maxcardoso/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/maxcardoso/Projects/Service-schedule/frontend/src/pages/admin/Users.jsx (reference CRUD pattern -- follow this EXACTLY for structure, imports, dialog pattern, mutations, toast)
@/Users/maxcardoso/Projects/Service-schedule/frontend/src/lib/api.js (apiFetch wrapper)
@/Users/maxcardoso/Projects/Service-schedule/src/routes/admin/services.js (existing admin service routes)
@/Users/maxcardoso/Projects/Service-schedule/src/services/serviceService.js (existing service layer)
@/Users/maxcardoso/Projects/Service-schedule/prisma/schema.prisma (Service model: id, name, description, durationMin, price, active)
@/Users/maxcardoso/Projects/Service-schedule/frontend/src/App.jsx (router config)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend GET /api/admin/services list endpoint</name>
  <files>src/services/serviceService.js, src/routes/admin/services.js</files>
  <action>
  1. In `src/services/serviceService.js`, add a `listAllServices()` function (export it) that returns ALL services (not just active), ordered by name ascending. Include id, name, description, durationMin, price, active, createdAt. Use `prisma.service.findMany` with no `where` filter.

  2. In `src/routes/admin/services.js`:
     - Import `listAllServices` from the service layer
     - Add `router.get('/', asyncHandler(async (req, res) => { ... }))` that calls `listAllServices()` and returns `{ data: services }`
     - IMPORTANT: Place the GET '/' route BEFORE the PUT '/:id' and PATCH '/:id/deactivate' routes to avoid route conflicts

  3. Also add a PUT route that supports toggling active status: the existing PUT `/:id` route already accepts partial body via `serviceBodySchema.partial()`, but it does NOT include `active` in the schema. Extend the PUT body schema to accept `active: z.boolean().optional()` and update the `updateService` function in serviceService.js to handle the `active` field (same pattern as professionalService.updateProfessional).
  </action>
  <verify>
  Run from project root:
  ```
  curl -s http://localhost:3150/api/admin/services -H "Authorization: Bearer $TOKEN" | head -c 200
  ```
  Should return `{"data":[...]}` with all services including inactive ones.
  </verify>
  <done>GET /api/admin/services returns all services (active + inactive). PUT /api/admin/services/:id accepts {active: bool} for toggling.</done>
</task>

<task type="auto">
  <name>Task 2: Build Services.jsx CRUD page following Users.jsx pattern</name>
  <files>frontend/src/pages/admin/Services.jsx, frontend/src/App.jsx</files>
  <action>
  Create `frontend/src/pages/admin/Services.jsx` following the EXACT same pattern as Users.jsx. Specifically:

  **Page structure:**
  - Header with title "Services" and "New Service" button (Plus icon)
  - Table with columns: Name, Duration, Price, Status, Actions
  - Actions column: Switch for active toggle + Pencil edit button (same layout as Users.jsx)
  - StatusBadge component (reuse same pattern: green for active, gray for inactive)
  - SkeletonRow for loading state
  - Empty state row when no services
  - Error banner for fetch failures

  **CreateServiceDialog (controlled, open + onOpenChange):**
  - Fields: name (Input, required), durationMin (Input type="number", min=5, placeholder "Duration in minutes"), price (Input type="number", step="0.01", placeholder "0.00")
  - Mutation: POST to `/admin/services` with `{ name, durationMin: Number(durationMin), price: Number(price) }`
  - On success: invalidateQueries(['services']), toast.success, close dialog, reset form
  - Validation: all fields required, durationMin >= 5, price > 0

  **EditServiceDialog (controlled, receives `service` prop):**
  - Same fields as create but pre-populated from service prop
  - Mutation: PUT to `/admin/services/${service.id}` with updated fields
  - On success: invalidateQueries(['services']), toast.success, close dialog

  **Main component:**
  - `useQuery({ queryKey: ['services'], queryFn: () => apiFetch('/admin/services') })`
  - `const services = data?.data ?? []`
  - toggleActiveMutation: PUT to `/admin/services/${id}` with `{ active: !currentActive }`
  - Format price as currency: `R$ ${Number(service.price).toFixed(2)}`
  - Format duration: `${service.durationMin} min`

  **Wire into router (App.jsx):**
  - Import Services from `@/pages/admin/Services`
  - Add route `{ path: '/admin/services', element: <Services /> }` inside the ADMIN-only ProtectedRoute children, same nesting level as `/admin/users`
  </action>
  <verify>
  1. Run `cd /Users/maxcardoso/Projects/Service-schedule/frontend && npx vite build --mode development 2>&1 | tail -5` -- should compile without errors
  2. Verify Services.jsx exists and has CreateServiceDialog, EditServiceDialog exports
  3. Verify App.jsx has the /admin/services route
  </verify>
  <done>Services page renders at /admin/services with working create/edit dialogs and active toggle. Table shows name, duration, price, status. All mutations invalidate cache and show toast.</done>
</task>

</tasks>

<verification>
1. `GET /api/admin/services` returns all services including inactive
2. `PUT /api/admin/services/:id` with `{active: false}` toggles service status
3. Frontend builds without errors
4. Services page follows identical pattern to Users.jsx (dialogs, mutations, table layout)
5. Route `/admin/services` is protected by ADMIN role
</verification>

<success_criteria>
- Admin can view all services in a table at /admin/services
- Admin can create a service via dialog (name, duration, price)
- Admin can edit a service via dialog
- Admin can toggle active/inactive with one-click switch
- Changes reflect immediately in table without page reload
</success_criteria>

<output>
After completion, create `.planning/phases/06-services-professionals-management/06-01-SUMMARY.md`
</output>
